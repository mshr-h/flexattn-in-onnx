# Agent Notes

- Use the repository-local virtual environment for Python commands:
  `uv run python`.
- Prefer running scripts and checks through that interpreter, for example:
  `uv run python model.py`.
